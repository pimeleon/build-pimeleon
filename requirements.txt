# MkDocs Material and dependencies for Pi Router documentation
# Install with: pip install -r requirements.txt

# Core MkDocs (pinned <2.0 — MkDocs 2.0 removes plugins and breaks Material theme)
mkdocs>=1.5.3,<2.0

# Material theme
mkdocs-material>=9.5.0

# Plugins
mkdocs-git-revision-date-localized-plugin>=1.2.0
mkdocs-minify-plugin>=0.7.0

# Extensions
pymdown-extensions>=10.5
Pygments>=2.17.0

# Optional but recommended
pillow>=10.1.0  # For social cards
cairosvg>=2.7.0  # For SVG support in social cards
