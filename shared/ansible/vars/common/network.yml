---
# Network configuration defaults
# These fix the undefined variables in network-setup.yml

# Interface naming
wan_interface: "eth0"
wifi_interface: "wlan0"
mgmt_interface: "eth0:0"

# Ethernet availability (override for Zero W)
ethernet_available: true

# WiFi AP network settings
wifi_ap_ip: "192.168.42.1"
wifi_ap_netmask: "255.255.255.0"
wifi_ap_network: "192.168.42.0/24"

# LAN Aliases (Tied to primary WiFi AP)
lan_ip: "{{ wifi_ap_ip }}"
lan_network: "{{ wifi_ap_network }}"

# Management network
mgmt_ip: "172.16.0.1"
mgmt_netmask: "255.255.255.0"

# DHCP configuration
wifi_dhcp_range_start: "192.168.42.100"
wifi_dhcp_range_end: "192.168.42.200"
dhcp_lease_time: "12h"

# WiFi AP configuration
wifi_ssid: "pimeleon"
wifi_passphrase: "changeme123"
wifi_channel: 6
wifi_hw_mode: "g"
wifi_country_code: "US"

# NTP servers for DHCP clients
ntp_servers:
  - "0.pool.ntp.org"
  - "1.pool.ntp.org"
  - "2.pool.ntp.org"

# DNS domain
dns_domain: "pimeleon.local"
domain_name: "{{ dns_domain }}"

# Internal Service Ports
pihole_port: 5553

# Template Context Facts
default_ip: "{{ wifi_ap_ip }}"
dns_local: "127.0.0.1"
dns_internal: "{{ wifi_ap_ip }}"
hostname: "pimeleon"

# Squid Proxy Configuration
squid_http_port: 3128
squid_transparent_port: 3129
squid_https_port: 31443
